server {
    listen 80;
    listen [::]:80;
    server_name dashboard.sellingerp.com;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name dashboard.sellingerp.com;

    ssl_certificate     /etc/letsencrypt/live/dashboard.sellingerp.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dashboard.sellingerp.com/privkey.pem;

    client_max_body_size 25m;

    location = /albums {
        return 301 /albums/;
    }

    location /albums/ {
        rewrite ^/albums/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300;
    }

    location = / {
        return 301 /albums/;
    }
}
